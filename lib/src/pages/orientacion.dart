import 'package:flutter/material.dart';

class OrientacionPage extends StatelessWidget {
  const OrientacionPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Orientacion'),
    );
  }
}
